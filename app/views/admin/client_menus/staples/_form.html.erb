<%= form_for(@staple, url: url) do |f| %>
  <div class="form-item">
    <%= f.label :name, class: 'label' %>
    <%= f.text_field :name, class: 'input--text required' %>
  </div>

  <div class="form-actions">
    <%= f.submit 'Save', class: 'button button--submit' %>

    <button data-modal-close class="link">
      Cancel
    </button>

    <% if @staple.persisted? %>
      <%= link_to 'Delete',
          admin_client_menu_staple_path(@client_menu, @staple),
          class: 'link link--danger',
          data: {
            confirm: 'Are you sure?',
            turbo_method: :delete
          }
      %>
    <% end %>
  </div>
<% end %>

<script>
  $('.edit_client_menu_staple, .new_client_menu_staple').validate();
</script>
