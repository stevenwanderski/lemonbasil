<div class="admin-header">
  <h1 class="capitalize"><%= @page.friendly_slug %></h1>
</div>

<%= form_for(@page, url: admin_update_page_path(slug: @page.slug)) do |f| %>

  <div class="form-item">
    <%= f.text_area :body, class: 'input--textarea' %>
  </div>

  <div class="form-item">
    <p class="hint">
      <a href="https://www.markdownguide.org/cheat-sheet/" target="_blank">
        <strong>💁</strong> Click here to see formatting tips!
      </a>
    </p>
  </div>

  <%= f.submit 'Save', class: 'button' %>
<% end %>
